# Scratch Cart Test App

To run the tests, execute the following command:
```
./gradlew test
```

The project includes static analysis tools ktlint and detekt. To run them, execute the following command:
```
./gradlew lintAll
```
